// shuffle the queue

module.exports = {
    data: new SlashCommandBuilder()
        .setName('shuffle')
        .setDescription('Toggles shuffling songs in the music queue.')
        .setDMPermission(false),
    async execute(interaction){
        
    }
}